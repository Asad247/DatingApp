{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "rootDir": "src", // <--- Add this line
    "types": []
  },
  "include": [
    "src/**/*.ts" // Ensure this points to src
  ],
  "exclude": [
    "src/test.ts",
    "src/**/*.spec.ts"
  ]
}